aside.navigation
  nav.navigation__wrapper
    ul.navigation__list
      each item in navigation.data.list
        li.navigation__list__item
           a.navigation__list__link.--navigation__link(href=link(item.link))=item.text
